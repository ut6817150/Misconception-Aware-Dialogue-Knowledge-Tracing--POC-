A student with a known general misconception is working through a math problem with a tutor. The student has a characteristic, recurring misconception; track THAT, turn by turn. Label each student turn for whether the general misconception shows.

Problem: {problem}

General misconception (track this): {profile}

For each student turn, output one label:
- present: the general misconception is exhibited in the turn.
- absent: the student correctly does the reasoning the general misconception would corrupt, with no sign of it.
- not_evidenced: the turn does not involve the general misconception's reasoning at all.

Output only a JSON object keyed by turn number, each value {{"label": ..., "reason": ...}}. No other text.
